// ThemeProvider stub — no-op. Dark mode is enforced via bp5-dark class on <html>.
// Kept to satisfy any remaining import references without breaking the build.
export function ThemeProvider() {
  return null;
}
