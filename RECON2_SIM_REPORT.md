# RECON2_SIM_REPORT — lane A: N1 anchor selector + F-VMP-1 (worktree, no server/build)

2026-07-21. Worktree `wt-recon2-sim`, branch `recon2/sim-anchor`, base **2c0311f** (recon-v1).
5 code commits (+ this report as a docs commit), tree clean, NOTHING built, no server contact, no push. Left in place for the
merge mini-pass.

## Step 0 — housekeeping

- `wt-recon-scen` REMOVED (needed the Windows long-path `\\?\` delete for node_modules, then
  `git worktree prune`); branch `recon/scen` retained (pushed at recon-v1).
- `C:\Users\infomax\sim2-design\SIM2_DESIGN.md` **does not exist — recorded as lost**
  (matches RECON2_DESIGN.md's doc note). Proceeded on SIM2_REPORT.md alone; no docs copy
  commit (nothing to copy).

## Commits (all on `recon2/sim-anchor`)

| Commit | What |
|---|---|
| a198a82 | Pre-change golden request fixture (identity evidence) — generated by running **2c0311f's untouched `buildSimulateRequest`** over the owner-representative config (`n1-golden-config.ts`: every request-shaping param nonzero — spreads −5/12/18, credit 3/4/7/9, IRS 4, touched waypoints 45/120, two −25bp 금통위 events, σ3.5, fundingStepping, explicit fundingRate). Committed BEFORE any code change. |
| b622bff | **T1 core**: `anchorTenor` param (optional, absent ≡ 3Y) + re-expression in `buildSimulateRequest` (`base_wire = X − s_rel(τa)`, `wp_wire = wpAnchor × base_wire/X`, exported `tenorSpreadAt` from the generateShockCurves node formula) + `anchorConversionError` floor + Configure selector/dynamic labels/alert + runCurrent guard + `lastRunAnchorTenor`. |
| 4c12912 | **T1 preview/drag + T2**: curve-view anchor gating params-driven (chip auto-select, marker/drag host, captions); Results chip anchor-native; DTO "3Y 관측" doc labeling. |
| 20fad59 | **T3**: 불일치 raw signed Δ (`formatRawDeltaKrw`), comparison math/threshold untouched. |
| 390cef4 | Lint/staleness fix: render-phase reconcile for chip auto-select (was setState-in-effect), `anchorTenor` added to the pathSeries memo deps (real staleness bug), unused import dropped. |

## T1 evidence

- **Identity pin (mandatory)** — `n1-golden-request.json` (committed pre-change, a198a82) is
  reproduced **byte-for-byte** by the post-change builder with `anchorTenor` ABSENT and with
  explicit `"3Y"`; the 3Y path even keeps the waypoint array by REFERENCE (pinned). The wire
  contract is unchanged — no new request fields.
- **Conversion exactness** — 1Y/5Y/10Y pins: wire `baseShockBp = X − s_rel(τa)` exactly; the
  anchor pillar's wire 국채 node lands on the designed X; waypoints rescale by
  `base_wire/X`; the absolute short-end (BOK) shock is anchor-invariant (pinned equal to the
  legacy build).
- **0.5bp floor** — `anchorConversionError`: X==0 under a non-3Y anchor blocked (its own
  message); `|base_wire| < 0.5bp` blocked naming the numbers (10Y with X=12=spread10y →
  "상쇄"; 12.4 blocked / 12.6 passes — boundary pinned). Enforced at BOTH layers:
  Configure (role=alert + disabled CTA, pinned) and `runCurrent` (markError, **API provably
  not called** — pinned with a mocked transport). Anchor 3Y NEVER blocks, including the
  legacy X==0 state (pinned) — the silent-disable gotcha is unchanged there by ruling, and
  unreachable via the anchor route. A NaN-guard keeps the live preview finite on transient
  degenerate states (pinned).
- **Preview/M2 consistency — verified, not re-derived**: the 시계열형 panel and lane-B's M2
  evaluator both consume `buildSimulateRequest`'s wire request (no second math path); the
  round-trip pin runs the CONVERTED 5Y request through `createPathEvaluator` and gets the
  designed waypoints back on the 국채 5Y pillar (18/22/30 at D+45/120/180).

## T2 evidence

- Chip: "국고 {τa} 목표" + X reconstructed from the wire 국채 curve at the anchor node
  (exact node, no interpolation; `lastRunAnchorTenor` recorded at build time). Pinned for 5Y
  (+30bp shown, wire's +26.57bp provably NOT shown) and for pre-N1 results (null anchor →
  3Y label, wire figure — same value by identity).
- "3Y 관측" labeling: grep census found **zero live consumers** of the `_ktb3y_bp`-fed
  fields (`rate_path`/`ratePaths`) — dormant since HARDEN-1 removed the fan (only the DTO
  type + lane-B fixtures mention them). The labeling therefore lands on the DTO contract doc
  (ratePaths = "3Y-관측 cross-section … a revived consumer must label it '3Y 관측'"); there
  is no rendered surface to label, honestly reported rather than invented.

## T3 evidence

- Mismatch line now renders `… 예상 X vs 엔진 Y (Δ ₩+2.42)`-style raw signed difference,
  full precision (grouped 2dp at won scale; 4dp below ₩0.005 so a 불일치 can never display
  Δ ₩0). Threshold, comparison math, engine, fixtures: untouched (the nudged-fixture pin
  proves the verdict flips only from the engine-side nudge and the projected lane is
  intact). The component-test output showing the rendered footer is the floor-error/labeling
  evidence channel (no build allowed in this lane, so no screenshots — component-rendered
  DOM assertions are the evidence, per the report mandate).

## Gates (final head 390cef4)

| Gate | Result |
|---|---|
| tsc | clean |
| vitest | **367 passed / 0 failed** = 345 (base) + **22 new** |
| eslint | **13E / 21W** (= gate ≤13E/21W; a mid-lane regression to 14E/22W was caught and fixed in 390cef4) |
| guards | all green (in-suite; cashflow reuse guard, lockdown, contrast, canvas-var, zorder, hex) |
| tree | clean |
| forbidden surfaces | **zero diffs** — `git diff --name-only 2c0311f..HEAD` = 17 files, ALL under `src/features/simulation/**` (+ `.impeccable/hook.cache.json`, the sanctioned hook cache). No Home/rates-history/daily-recon/matrix-grid/BE files. |
| build / push / servers | none, none, untouched (owner demo servers never contacted) |

### New-test enumeration (N = 22, never a bare total)

`scenario-curves.test.ts` (+11): golden identity ×2 (absent / explicit-3Y + waypoint
reference identity) · 5Y conversion exactness · 10Y+1Y s_rel exactness · short-end
preservation · path-matrix round trip · NaN-guard · floor: 3Y-never-blocks ×1, X==0 non-3Y
×1, 0.5bp boundary ×1 · tenorSpreadAt formula ×1.
`use-simulation.test.tsx` (+2): degenerate → no API call + honest error · valid 10Y →
converted wire + lastRunAnchorTenor recorded.
`configure-stage.test.tsx` (+3): selector default/choices + legacy copy intact · 5Y dynamic
relabeling + vs-3Y spread-label pin · floor blocks run + clears on fix.
`curve-view-panel.test.tsx` (+2): anchor-5Y gating (chip/caption/markers) · post-mount
anchor switch (auto-select, no deselect).
`results-stage.test.tsx` (+2): anchor-native chip · pre-N1 fallback.
`scenario-recon-panel.test.tsx` (+2): formatRawDeltaKrw units · rendered raw-Δ on a nudged
fixture.

## Deferred / notes for the merge pass

1. Lane B (RECON2-RH, `recon2/rh-deltabp` @ a4462c8, 363/0 + 13E per their report) and this
   lane (367/0 + 13E) both sit on 2c0311f with disjoint surfaces — expected union
   345 + 22 + 18 = **385** if their +18 is disjoint (their report says +18 over 345; the
   merge pass owns the union derivation and the single build/restart window).
2. The owner rulings consumed here (option (a), 0.5bp floor, spreads-vs-3Y, anchor-native
   chip) came from the session brief; RECON2_DESIGN.md's open rulings #3/#4 (N2 ship order,
   N2 default tenor) belong to lane B.
3. `ratePaths`/`rate_path` remain dormant; if a consumer is ever revived, the DTO doc now
   mandates the "3Y 관측" label (T2).
4. Not done anywhere (pre-existing ledger): per-sector realized buckets (BE), mirror
   unification, s20 sign-offs, SIM-2 flag ① key-count instrumentation.
